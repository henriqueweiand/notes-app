import { styled } from "..";

export const Container = styled('input', {
    minWidth: '100%',
    display: 'inline-block',
    background: '$gray300',
    border: '1px solid $gray800',
    borderRadius: 5,
    padding: '0.5rem 1rem',
    color: '$gray900',
})